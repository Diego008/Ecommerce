@model IEnumerable<Ecommerce.Models.Admin>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewBag.Title = "Index";
}

@{
    Ecommerce.Models.Admin admin = Ecommerce.Repositories.Funcoes.GetAdmin();
    if(admin != null){
        <h2>Estatísticas</h2>

        <div class="row">
            <div class="col-sm-6 col-md-4">
                <a href="@Url.Action("Index","Categorias")" class="thumbnail text-center icons">
                    <i class="fa fa-book fa-5x"></i>
                    <div class="caption">
                        <h3>
                            @ViewBag.Categorias
                        Categorias Cadastradas
                    </h3>
                </div>
            </a>
        </div>

        <div class="col-sm-6 col-md-4">
            <a href="#" class="thumbnail text-center icons">
                <i class="fa fa-users fa-5x"></i>
                <div class="caption">
                    <h3>
                        @ViewBag.Administradores
                    Administradores Cadastrados
                </h3>
            </div>
        </a>
    </div>

            <div class="col-sm-6 col-md-4">
                <a href="#" class="thumbnail text-center icons">
                    <i class="fa fa-user fa-5x"></i>
                    <div class="caption">
                        <h3>
                            @ViewBag.Usuarios
                            Usuarios Cadastrados
                        </h3>
                    </div>
                </a>
            </div>
</div>
    }
    else
    {
        <script>
            window.location.href = "@Url.Action("Logar", "Publico")";
        </script>
    }
}
